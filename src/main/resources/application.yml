spring:
  resources:
    static-locations: classpath:/static/
  mvc:
    static-path-pattern: /static/**
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://106.12.160.148:3306/houjie?useUnicode=true&charaterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
# mybatis
mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    com:
      itzhang: debug

