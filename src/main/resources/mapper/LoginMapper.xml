<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itzhang.mapper.LoginMapper">
    <select id="loginCheck" resultType="com.itzhang.bean.Emp">
        select id, username, password, real_name realName
        , role, state, img , create_time createTime from emp where username= #{username};
    </select>

</mapper>